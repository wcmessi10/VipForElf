spring:
  datasource:
    url: jdbc:h2:mem:testdb  # 메모리 H2 DB
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트마다 새로 테이블 생성/삭제
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
